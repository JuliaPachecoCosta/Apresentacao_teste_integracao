const form = document.getElementById("loginForm"); form.addEventListener("submit", function(event) { event.preventDefault(); const email = document.getElementById("email").value; const senha = document.getElementById("senha").value; const mensagem = document.getElementById("mensagem"); if ( email === "usuario@email.com" && senha === "123456" ) { mensagem.textContent = "Login realizado com sucesso!"; } else { mensagem.textContent = "E-mail ou senha incorretos."; } });